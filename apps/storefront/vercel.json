{
   "$schema": "https://openapi.vercel.sh/vercel.json",
   "framework": "nextjs",
   "installCommand": "cd ../.. && npm install && npm install --prefix apps/storefront",
   "buildCommand": "npm run vercel-build"
}
